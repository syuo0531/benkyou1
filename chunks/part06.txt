;const c=document.createElement('div');c.className='card boss';c.innerHTML=`<div class="boss-head"><div class="boss-icon" style="background:${t.color}">${t.icon}</div><div><div class="boss-name">${escapeHtml(active.name)}</div><span class="tier">${t.label}${active.reviewStage>=0?'・復習':''}</span><div style="margin-top:5px"><span class="elem-badge ${el.class}">${el.icon} 属性：${el.label}</span><span class="elem-badge ${wk.class}">${wk.icon} 弱点：${wk.label}</span></div></div></div><div class="hpbar"><i style="width:${pct}%"></i></div><div class="boss-bottom"><span>HP ${Math.max(0,Math.round(active.hp))} / ${active.maxHp}</span><span>報酬 +${active.reward}XP</span></div><div class="combat-note">現在の武器：<b>${weapon?escapeHtml(weapon.name):'未装備'}</b> ${weapon?`<span class="elem-badge ${elementInfo(weapon.element).class}">${elementInfo(weapon.element).icon}${elementInfo(weapon.element).label}</span>`:''}<br>属性倍率：<span class="damage-preview">×${mult.toFixed(2)}</span>${mult>1?'（弱点！）':mult<1?'（耐性あり）':''}<br>反撃率：<span class="damage-preview">${Math.round(counterRateForBoss(active)*100)}%</span> / 基本反撃 ${counterBaseDamage(active)}前後</div><div class="boss-actions"><button class="btn ghost" data-edit-boss="${active.id}">編集</button><button class="btn danger" data-remove-boss="${active.id}">破棄</button></div>`;area.appendChild(c);const others=state.bosses.filter(b=>b.id!==active.id);if(others.length){const q=document.createElement('div');q.className='queue';others.forEach(b=>{const r=document.createElement('div');r.className='queue-row';const e=elementInfo(b.element);r.innerHTML=`<span>${escapeHtml(b.name)} <span class="muted">HP ${Math.round(b.hp)}/${b.maxHp}</span> <span class="elem-badge ${e.class}">${e.icon}${e.label}</span></span><button class="btn" data-target="${b.id}">対象にする</button>`;q.appendChild(r)});area.appendChild(q)}}
function renderQuests(){const area=document.getElementById('questList');area.innerHTML='';if(!state.quests.length){area.innerHTML='<div class="card addbox muted" style="text-align:center">受注中の依頼はありません。</div>';return}state.quests.forEach(q=>{const info=DIFF[q.diffKey]||DIFF.normal,c=document.createElement('div');c.className='quest '+(q.diffKey==='hard'?'hard':q.diffKey==='epic'?'epic':'');c.innerHTML=`<div class="quest-top"><div class="quest-name">${escapeHtml(q.name)}</div><span class="tag">${info.label}</span></div><div class="quest-meta">予定 ${q.minutes||30}分 ・ 基本 ${q.xp}XP</div><div class="quest-actions"><button class="btn" data-edit-quest="${q.id}">編集</button><button class="btn primary" data-clear="${q.id}">達成</button></div>`;area.appendChild(c)})}
function renderReviews(){const rows=[document.getElementById('homeReview'),document.getElementById('reviewList')];for(const area of rows){area.innerHTML='';const list=[...state.reviewQueue].sort((a,b)=>a.dueDate.localeCompare(b.dueDate));if(!list.length){area.innerHTML='<div class="card addbox muted" style="text-align:center">予定されている復習はありません。</div>';continue}list.slice(0,area.id==='homeReview'?3:99).forEach(r=>{const d=document.createElement('div');d.className='review-row';d.innerHTML=`<div><b>${escapeHtml(r.name)}</b><small>${r.dueDate} ・ ${REVIEW_DAYS[r.stage]}日後復習 ・ HP ${r.maxHp}</small></div><span class="gold">${r.dueDate<=dateKey()?'出現':'待機'}</span>`;area.appendChild(d)})}}
function renderGear(){const eff=gearEffects(),slotArea=document.getElementById('gearSlots');slotArea.innerHTML='';SLOTS.forEach(([k,label])=>{const g=gearById(state.equipped[k]);const d=document.createElement('div');d.className='slot-pill'+(g?' filled':'');d.textContent=g?`${label}\n${g.name}`:label+'\n—';slotArea.appendChild(d)});document.getElementById('gearSkills').innerHTML=`<div class="skill-line"><span>装備スキル：獲得XP</span><b>+${Math.round(eff.xp*100)}%</b></div><div class="skill-line"><span>高難度ボーナス</span><b>+${Math.round(eff.hardXp*100)}%</b></div><div class="skill-line"><span>追加素材率</span><b>+${Math.round(eff.material*100)}%</b></div><div class="skill-line"><span>総防御力</span><b>${eff.defense}</b></div>`;let setText='セット効果なし',setName='旅立ちの装い';const sets=[['dragon','古龍シリーズ','3部位：XP+10%','5部位：XP+20% / 高難度+15% / 防御+20'],['storm','雷帝シリーズ','3部位：XP+6%','5部位：高難度+10%'],['frost','氷狼シリーズ','3部位：素材+8%','5部位：防御+18'],['blaze','緋炎シリーズ','3部位：高難度+8%','5部位：XP+7%'],['wolf','黒狼シリーズ','3部位：XP+5%','5部位：素材+12%'],['bone','骨鎧シリーズ','3部位：防御+8','5部位：XP+3%']];for(const [k,n,a,b] of sets){if(eff.setCount[k]>=3){setName=n;setText=eff.setCount[k]>=5?b:a;break}}document.getElementById('setName').textContent=setName;document.getElementById('setBonus').textContent=setText;applyVisuals();renderGearTabs();renderGearGrid();renderGearDetail();const mats=document.getElementById('materials');mats.innerHTML='';Object.keys(MATS).forEach(k=>{const d=document.createElement('div');d.className='mat';d.innerHTML=`${MATS[k]} <b>×${state.inventory[k]||0}</b>`;mats.appendChild(d)})}
function applyVisuals(){const op=(id,on)=>document.getElementById(id).style.opacity=on?'1':'.14';op('visWeapon',!!state.equipped.weapon);op('visHead',!!state.equipped.head);op('visChest',!!state.equipped.chest);op('visArmL',!!state.equipped.arms);op('visArmR',!!state.equipped.arms);op('visWaist',!!state.equipped.waist);op('visLegL',!!state.equipped.legs);op('visLegR',!!state.equipped.legs);op('visCharm',!!state.equipped.charm);op('visCharmDrop',!!state.equipped.charm);const sets=SLOTS.map(([s])=>gearById(state.equipped[s])?.set).filter(Boolean);let color='#421b17';if(sets.includes('dragon'))color='#5f1717';else if(sets.includes('storm'))color='#463d18';else if(sets.includes('frost'))color='#24465a';else if(sets.includes('blaze'))color='#6a2416';else if(sets.includes('wolf'))color='#253942';else if(sets.includes('bone'))color='#574a3b';document.getElementById('visCape').style.fill=color;document.getElementById('visCape').style.opacity=sets.length?'.9':'.25'}
function renderGearTabs(){const area=document.getElementById('gearTabs');area.innerHTML='';const groups=[['weapon','武器'],['head','頭'],['chest','胴'],['arms','腕'],['waist','腰'],['legs','脚'],['charm','護石']];groups.forEach(([k,l])=>{const b=document.createElement('button');b.className='btn gear-tab '+(gearSlot===k?'gold':'');b.dataset.gearTab=k;b.textContent=l;area.appendChild(b)})}
function renderGearGrid(){const area=document.getElementById('gearGrid');area.innerHTML='';const list=GEAR.filter(g=>g.slot===gearSlot);if(!selectedGear||!list.some(g=>g.id===selectedGear))selectedGear=state.equipped[gearSlot]||list[0]?.id||null;list.forEach(g=>{const made=state.craftedGear.includes(g.id),eq=state.equipped[g.slot]===g.id,b=document.createElement('button');b.type='button';b.className='gear-item '+(selectedGear===g.id?'selected ':'')+(made?'':'locked');b.dataset.gear=g.id;const el=g.slot==='weapon'?elementInfo(g.element):null;b.innerHTML=`<div class="gicon">${g.icon}</div><b>${escapeHtml(g.name)}</b><small>${'★'.repeat(g.rarity)}${eq?' ・ 装備中':made?' ・ 作成済み':' ・ 未作成'}</small>${el?`<small class="${el.class}">${el.icon} ${el.label}属性 ・ 攻撃倍率×${(g.power||1).toFixed(2)}</small>`:`<small>防御 ${g.defense||0}</small>`}`;area.appendChild(b)})}
function renderGearDetail(){const area=document.getElementById('gearDetail'),g=gearById(selectedGear);if(!g){area.innerHTML='<div class="muted">装備を選択してください。</div>';return}const made=state.craftedGear.includes(g.id),eq=state.equipped[g.slot]===g.id,cost=Object.entries(g.cost).map(([m,n])=>`${MATS[m]} ${state.inventory[m]||0}/${n}`).join(' ・ '),skill=Object.entries(g.skills).map(([k,v])=>`${k==='xp'?'獲得XP':k==='hardXp'?'高難度XP':'素材率'} +${Math.round(v*100)}%`).join(' / '),el=g.slot==='weapon'?elementInfo(g.element):null;area.innerHTML=`<div class="detail-head"><div class="detail-icon">${g.icon}</div><div><div class="detail-name">${escapeHtml(g.name)}</div><div class="stars">${'★'.repeat(g.rarity)}${'☆'.repeat(5-g.rarity)}</div>${el?`<div><span class="elem-badge ${el.class}">${el.icon} ${el.label}属性</span> 攻撃倍率 ×${(g.power||1).toFixed(2)}</div>`:`<div>防御力 ${g.defense||0}</div>`}<div class="skills">${skill||'固有スキルなし'}</div></div>